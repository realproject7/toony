{
  "name": "toony",
  "license": "PolyForm-Noncommercial-1.0.0",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.33.3",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "turbo run build",
    "lint": "biome check .",
    "format": "biome format --write .",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "scan": "node scripts/scan-public-safety.mjs",
    "no-stub": "node scripts/check-no-stub.mjs",
    "render-inputs": "node scripts/check-render-inputs.mjs",
    "task-outputs": "node scripts/check-task-outputs.mjs",
    "check": "pnpm run typecheck && pnpm run lint && pnpm run scan && pnpm run no-stub && pnpm run render-inputs && pnpm run task-outputs",
    "verify-release": "./scripts/verify-release.sh",
    "test:verification": "node --test scripts/__tests__/*.test.mjs"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.0",
    "turbo": "2.9.18",
    "typescript": "6.0.3"
  }
}
