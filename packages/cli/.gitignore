# Generated by the CLI build (scripts/build.mjs + scripts/bundle-studio.mjs).
# The bundled JS, copied font/workflow assets, and the standalone Studio build
# are produced from source at build time and packed into the npm tarball; they
# are never committed.
dist/
dist-test/
assets/
studio/
*.tgz
